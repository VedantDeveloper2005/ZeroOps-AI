# The control plane reads every application setting directly from Azure Key
# Vault. These two non-secret settings are the minimum bootstrap required by
# App Service, Container Apps, and the isolated worker.
APP_ENV=production
AZURE_KEYVAULT_URL=https://<your-key-vault>.vault.azure.net/

# Optional only when using a user-assigned managed identity. Do not set a
# client secret; DefaultAzureCredential authenticates with the managed identity.
# AZURE_CLIENT_ID=<managed-identity-client-id>

# Keep this file value-only for bootstrap. All application settings, including
# PORT, FRONTEND_URL, DATABASE_URL, JWT_SECRET, SMTP, OAuth, AI, Stripe,
# Twilio, worker callbacks, and operational limits belong in Key Vault.
# See ../docs/production-key-vault.md for the naming convention and setup.

# ---------------------------------------------------------------------------
# NVIDIA Build API smoke-test settings (testing / development only).
# The application never reads NVIDIA_API_KEY as a workload fallback. Do not add
# this generic key to the production application configuration.
# ---------------------------------------------------------------------------
# NVIDIA_API_KEY=  # local smoke script only; workload routes never inherit it
# NVIDIA_ENDPOINT=https://integrate.api.nvidia.com/v1
# NVIDIA_MODEL=z-ai/glm-5.2

# AI workload routes — repository analysis
# AI_REPOSITORY_PROVIDER=nvidia
# AI_REPOSITORY_ENDPOINT=https://integrate.api.nvidia.com/v1
# AI_REPOSITORY_MODEL=z-ai/glm-5.2
# AI_REPOSITORY_API_KEY=
# AI_REPOSITORY_FALLBACK_PROVIDER=groq
# AI_REPOSITORY_FALLBACK_ENDPOINT=https://api.groq.com/openai/v1
# AI_REPOSITORY_FALLBACK_MODEL=openai/gpt-oss-120b
# AI_REPOSITORY_FALLBACK_API_KEY=
# AI_REPOSITORY_FALLBACK_MAX_INPUT_CHARS=14000
# AI_REPOSITORY_FALLBACK_MAX_OUTPUT_TOKENS=800

# AI workload routes — Terraform generation
# AI_TERRAFORM_PROVIDER=nvidia
# AI_TERRAFORM_ENDPOINT=https://integrate.api.nvidia.com/v1
# AI_TERRAFORM_MODEL=z-ai/glm-5.2
# AI_TERRAFORM_API_KEY=
# AI_TERRAFORM_FALLBACK_PROVIDER=groq
# AI_TERRAFORM_FALLBACK_ENDPOINT=https://api.groq.com/openai/v1
# AI_TERRAFORM_FALLBACK_MODEL=openai/gpt-oss-120b
# AI_TERRAFORM_FALLBACK_API_KEY=
# AI_TERRAFORM_FALLBACK_MAX_INPUT_CHARS=14000
# AI_TERRAFORM_FALLBACK_MAX_OUTPUT_TOKENS=1000

# There is intentionally no generic GROQ_API_KEY. For a small local test, put
# the same newly rotated value into both explicitly named fallback settings if
# needed; production Functions resolve the two values from separate Key Vaults.
