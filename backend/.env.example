# FastAPI App Service
APP_ENV=production
HOST=0.0.0.0
PORT=8000
WEB_CONCURRENCY=1
GUNICORN_TIMEOUT=180

# CORS: set to the frontend App Service origin.
# Example: https://zeroops-web.azurewebsites.net
FRONTEND_ORIGIN=
CORS_ORIGINS=

# Optional integrations. Missing values keep demo-safe fallback behavior enabled.
OPENAI_API_KEY=
GITHUB_TOKEN=
AZURE_KEYVAULT_URL=

# Optional metadata for docs and frontend config.
ZEROOPS_BACKEND_URL=
