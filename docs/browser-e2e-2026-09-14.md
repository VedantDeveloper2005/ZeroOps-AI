# Chrome application test — September 14, 2026

Result: **End-to-end deployment not passing / completion blocked.**

Tested the hosted application at https://zeroopsai-v2.azurewebsites.net using the existing signed-in Chrome session. This was an application UI test, not a verification of VMSS process health or a full regression suite.

## Observed results

| Flow | Result | Evidence |
| --- | --- | --- |
| Existing authenticated session | Pass | Dashboard, project, and account pages loaded. Fresh login/logout was not tested. |
| GitHub repository list and branch selection | Pass | Connected GitHub account listed repositories; selecting `VedantDeveloper2005/demo` loaded `main`. |
| Source review | Pass | Review source returned Express.js metadata and a generated plan; Review infrastructure plan opened the correct project. |
| Plan persistence | Pass | Generated revision 2 appeared in Architecture and the dashboard approval queue. |
| Preflight | Pass with warnings | Revision 2: 6 passed, 2 warnings, 0 blocked. Warnings concern human approval and unvalidated subscription pricing. This does not establish deployment readiness. |
| Activity | Pass for persistence | Both the new plan-generation and preflight events appeared. An older `terraform.apply.completed` event is present; its provenance and Azure result were not independently verified. |
| Azure settings | Readable | UI reports a verified connection and complete App Service fields, targeting `zeroops-demo-workload-rg`, `asp-zeroops-workloads`, Central India. No credentials or settings changed. |
| Deployment execution | Blocked | Existing deployment `1e918195-6a92-45bd-865e-ad1aa02f1dad`, commit `d4130570`, remained queued throughout inspection. No persisted deployment logs or recorded application URL. Dashboard says it is waiting for the deployment worker. |
| Pipeline timeline | Fail | 48 stage entries; each named stage appears twice, both labelled Attempt 1. UI reports 14/48 resolved (29%), accounted for by skipped stages rather than demonstrated execution. Root cause not established. |
| Live log transport | Partial | UI says Live updates connected, but no logs exist. Connection status does not prove a worker ran. |
| Runtime monitoring | Unverified | Project page reports No telemetry received; historical windows are unavailable. |
| GitHub push automation | Not configured | Pipeline settings show No signing secret stored; automatic deployment checkbox is disabled and unchecked. Push-to-release flow cannot be exercised in this state. |
| Region consistency | Needs investigation | Project overview/dashboard/settings context display `eastus`; saved Azure target and generated plan display Central India. Whether the project region is stale has not been established. |

## Changes made by the browser test

- Reviewed `demo/main` through the import UI. This persisted architecture revision 2, which is awaiting approval, and an activity event.
- Ran and persisted preflight for revision 2.
- Did not approve the new revision, start another deployment, modify Azure settings, generate credentials, or edit source code.

Project: `1ed1b668-8968-4b2c-870b-8160dd061fe6`.

## Next debugging steps

1. Trace the queued deployment's `DeploymentJob` and its worker claim/lease. Local code creates this PostgreSQL job in `backend/main.py`; `worker/main.py` polls `PostgresJobQueue`. A running Terraform execution service alone does not establish that this application deployment consumer is running. Live process/configuration diagnosis remains required.
2. Compare API stage-attempt records with database rows and deployed schema constraints to determine why Attempt 1 appears twice. Preserve execution evidence; do not mask duplicates as a substitute for finding the cause.
3. Resume only through the legitimate worker/approval flow, then verify source, checks, build, application deployment, health, smoke tests, and the recorded live URL.
4. Configure and verify GitHub webhook delivery before testing a commit-triggered release.
5. Verify telemetry collection after a real successful release and reconcile the displayed project region with the actual target.

No successful application deployment, live health response, incremental release, or rollback was verified in this test. Do not present this run as a successful end-to-end rehearsal.
